# Cursor monitor shells are mortal — teach re-arm

Canon, Cursor orchestrator standing orders, `/vilya-chip` §3, Planner intake
complement, and Differences now name Cursor's teardown of long-running
`notify_on_output` shells. Duty: arm → assume mortal → re-arm on noticed death /
long gaps / missing expected signal (one REST check). Preserves #267: do not
kill/re-arm after every successful drain. Claude Code Monitor path stays
host-specific. ADR in `DECISIONS.md`. (#270)
