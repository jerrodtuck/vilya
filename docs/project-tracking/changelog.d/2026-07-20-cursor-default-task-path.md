# Cursor daytime Task/BoN default (#285)

`/orchestrator` + `/vilya-orch-cursor` teach Task/BoN worktree-first as the
daytime default (Task return + issue comment wake; mortal REST monitor). Worker A
three-step is collapsed fallback. Soft Overview / Ask Vilya / Planner lines for
Cursor optional Planner.
