# Cursor daytime Task/BoN default + Orch page (#285)

`/orch` (nav: Orch) + `/vilya-orch-cursor` teach Task/BoN worktree-first as the
daytime default (Task return + issue comment wake; mortal REST monitor). Worker A
three-step is collapsed fallback. Host toggle (`?host=cc|cursor`) filters
standing orders and path panels; Claude host shows spawn_task chip path.
`/orchestrator` permanently redirects to `/orch`. Soft Overview / Ask Vilya /
Planner lines for Cursor optional Planner.
