# /cursor-handoff skill: graduate Cursor three-step worker ritual

New user-level skill `skills/cursor-handoff/SKILL.md` (#247, epic #224;
graduates the #219 teach UI): the invoke target for Cursor's paste →
open-folder → Worker A dispatch. Standing-order prompt cards still seat
roles; Claude chips stay on `/chip` + `spawn_task`. Name is
`/cursor-handoff` — not `/chip-cursor`. No auto-handoff claimed.

Registry: skill lists under **Process** via folder discovery (same as
`/chip`). `/orchestrator` Cursor path panel + standing-orders intro name
`/cursor-handoff` as the invoke target without a second ritual. Skill is
live on `git pull` via the user-level junction; served copies regenerated
via `sync:skills`.
