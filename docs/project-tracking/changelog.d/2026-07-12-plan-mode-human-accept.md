# Plan mode requires human accept — daytime fallback

- **`/start-feature`** — Cursor Plan mode is operator-gated; no UI accept → plan on
  the issue / in-chat without a mode switch and stay on the execution model (do not
  pretend Plan mode ran). Model-switch stop only when Plan mode actually ran;
  night-shift skip unchanged.
- **Flows + Setup** — orchestrator / New brief prompts and Setup Models copy no
  longer imply an automatic Plan switch; GITHUB-PROJECTS Models note + generator
  aligned.
