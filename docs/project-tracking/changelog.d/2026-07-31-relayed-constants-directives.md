# Canon: relayed constants and directives

Load-bearing constants that arrive by relay stay approximations until confirmed
**operator-direct** or by **measurement**; a hedge marks a non-constant. Relayed
standing directives use **comply-then-verify** when safe/cheap and
**verify-before-comply** when destructive or risk-expanding. Record the evidence
channel: `operator-direct` · `measured` · `relayed via <session>`.

Surfaces: `/vl-chip` §2d (execute-time), orch seats (author + receive),
`/vl-plan` + `/vl-start-feature` kickoff authoring, `/vl-merge-pr` honesty bar,
Worker A/B + `/vl-cursor-handoff`, and planner/orch site prompt parity. Sibling
#319 (brief findings rebuttable by measurement) stays distinct; #313 stays a
separate PR. Receipt inspiration: Anduin 2026-07-22/23 (hedged cutoff hardened;
confidentiality directive via architect relay).
