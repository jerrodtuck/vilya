# Land docs/VISION.md + refresh README skills tree

`docs/VISION.md` created (#142): the system's intent document — founding
intent, the five-seat role model, the cardinality rule (one architect per
product board, one orchestrator per repo), current direction, and
non-goals — landed **verbatim** from the architect session's v2 authored
comment on #142 (both review amendments applied there; transcription
verified byte-identical against the comment body). Carries the
Created/Last-updated dating convention and its owning issue, and opens
with the VISION/canon boundary line: intent lives here, process stays in
`docs/project-tracking/GITHUB-PROJECTS.md` and each skill's `SKILL.md`.
This closes the gap where the canon's shared-files table and the Architect
page's ground-truth toolset both referenced a file that didn't exist.

README skills tree unstale'd (#147): the repo-tree diagram listed 8 skill
directories while `skills/` carries 11 — `chip/`, `prune/`, and `adr/`
added with one-phrase role annotations matching the existing entries;
tree verified one-to-one against `ls skills/`.
