# Canonize `vilya-*` skill names in docs

Append #257 ADR to `DECISIONS.md`. Update live skill-name teaching in `VISION.md`,
specs, and `GITHUB-PROJECTS.md` to `/vilya-*` invokes (hard cut; no submenu).
Historical `changelog.d/` fragments left as pre-rename history (#260; epic #257).
