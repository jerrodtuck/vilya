### Changed

- **Both orchestrator cards now carry the board-truth dispatch rule** — moving
  the issue to In Progress on the project board is a same-turn dispatch
  obligation, because GitHub Projects' built-in workflows only cover
  added→Todo and closed/merged→Done. The Claude Code orchestrator card folds
  it into the same-turn Monitor sentence ("do two things"); the Cursor
  orchestrator card gets a new "Your job:" bullet at the kickoff-comment /
  worker-session step. Architect and kickoff/worker cards unchanged. (#167)
