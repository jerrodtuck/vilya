# Site teaching surfaces use `/vilya-*` skill invokes

Role pages, prompt libraries, registry meta, and Setup crucible defaults now
teach `vilya-*` slugs and slash invokes after the skills/ hard cut. Site routes
`/planner` and `/night-shift` unchanged.
