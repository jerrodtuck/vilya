# Night-shift bring-up ship polish

- DISPATCH stage copy: cron is optional (commented in template), not “Active on this repo.”
- `show_full_output: false` after Anduin green overnight proof; template comment matches.
- Spec `night-agent-viz.md`: optional cron + timeout/high max-turns loop wording.
