Canon: investigate → findings + options on the issue → hard stop → operator pick →
implement. Daytime marks **Investigate-first / hard-stop** in the kickoff; unattended
uses `needs:decision`. Taught in `/chip`, `/start-feature`, `/planner`, night-shift, and
orchestrator/planner site cards. Does not replace ordinary `plan:ready` Planner flow. (#239)
