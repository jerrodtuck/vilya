### Internal

- Railway deployment config-as-code: root `package.json` proxy scripts,
  `railway.json` (build/start/healthcheck), `nixpacks.toml` (git in the
  image). Deploys as one service from the repo root; target
  https://vilya.jerrodtuck.com. (#9)
