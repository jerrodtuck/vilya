### Changed

- **/night-shift operator-first IA** — page bands are Header → Setup once → Run
  tonight → How it works → Troubleshoot. Mode A (Actions) uses numbered steps
  with expected results; Verify checklist gates first dispatch; bring-up walls
  live in a Troubleshoot symptom table. Pipeline map (`NightAgentMap`) demoted
  under How it works; Failure stage stays the visual ledger only.
