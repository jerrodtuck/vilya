# Crucible variants: method lineage credited to Cursor's "thermonuclear code review"

All five crucible variants (blazor, nextjs, fastapi, django, ml) now carry an
identical one-line credit in their stack-specific header block — "**Method
lineage:** the crucible method is inspired by Cursor's 'thermonuclear code
review' prompt." — placed outside the two hash-verified core subsections, so
the #175 contract holds: core prompt (`e91f1535`) and severity/reporting
contract (`707d8a67`) hash equal across all five, unchanged. Registry content
copies re-synced; the Overview "One method, per-stack dialects" callout closes
with the same credit.
