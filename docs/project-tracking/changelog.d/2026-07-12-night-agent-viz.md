# Night-agent visualization

Elevate `/night-shift` into a Flows-quality interactive 7-stage pipeline map
(Dispatch → Outputs + Failure layer), with safety/failure styling distinct from
the happy path. Typed stage modules + client island; zero new runtime deps.
Per-repo setup strip under the map covers Actions (canonical Bypass in
`claude_args`) and Desktop routines (explicit Bypass — not user-global default).
