### Added

- **`/vilya-architect` skill** — Product Architect seat for one product board:
  direction via issues, ADRs, and specs; never implements, dispatches chips, or
  merges. Host-agnostic; Copy on `/architect` remains fallback. Registry lists
  it under Process. Served copies via `sync:skills`. (#269, Refs #224)
