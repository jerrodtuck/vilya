### Changed

- **Canon chip-chain summary caught up with the #167/#176 card rules** — the
  dispatch step in `docs/project-tracking/GITHUB-PROJECTS.md` now names both
  same-turn obligations (arm a Monitor-tool monitor + move the issue to
  In Progress, since built-in workflows only cover added→Todo and
  closed/merged→Done) and pins the monitor to the Monitor tool (a background
  shell loop detects but cannot notify). Site content copy re-synced. (#196)
