### Added

- **`/planner` teaching surface** — first-class page with flow map + prompt
  library (standing orders, recall, plan item, fork, handoff), mirroring
  `/architect` and `/orchestrator`. One seat per repo; Fable; `needs:plan` →
  `plan:ready`; never implement/dispatch/merge. (#208, Refs #203)

### Changed

- **Home / nav / Ask Vilya** — Planner sits in the anytime loop after
  Architect and before Orchestrator (rolestrip, cards, nav link, routing
  lane for planning passes). Spec status bumped for #207 Done + #208 landing.
