### Changed

- **`HANDOFF.md` frozen as a historical snapshot** — a banner now marks it as
  the 2026-07-11 handoff from the original Cowork session (current truth:
  `docs/project-tracking/GITHUB-PROJECTS.md` and the skill-registry app), with
  `Created:`/`Last updated:` dating per the doc convention. The one now-false
  claim — that crucible skills carry `disable-model-invocation: true` — gets an
  inline correction (flag removed 2026-07-17, #91); other period statements
  (e.g. "7 skills") are left as-is under the banner. (#92)
