# Crucible variants: shared-core claim narrowed and made hash-verifiable

#175's chip proved the variants' header claim false by section-extraction
hash: only the core prompt (`e91f1535`) and the severity/reporting contract
(`707d8a67`) were byte-identical across the five shipped variants — SOLID
and the structural non-negotiables were already stack-adapted in blazor and
django, while fastapi and ml carried unadapted **React** examples inside
Python skills ("`as any`", "valid props", one-implementation *hooks*), and
crucible-fastapi's brownfield clause was still pure Next.js text. Operator
picked Option A (ADR, 2026-07-18): fastapi and ml's SOLID/non-negotiables
examples are now re-flavored to their stacks (rules unchanged), fastapi's
brownfield clause speaks FastAPI (flat `routers/`+`services/`+`crud/`
layer-cake mid-migration to domain packages; new work goes in a domain
package; the config/secret blocker never downgrades), and all five headers
state the precise claim — byte-identical core = core prompt +
severity/reporting contract; four stack-tuned pieces = the two stack
sections, the SOLID/non-negotiables examples, and the brownfield clause's
examples. Registry content copies re-synced for all five.
