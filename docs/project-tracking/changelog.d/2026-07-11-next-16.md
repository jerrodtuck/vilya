### Changed

- **Next.js 14.2 → 16.2 LTS (Turbopack) + React 19** — async `params`
  migration across the dynamic skill page, diff page, and raw-SKILL.md route;
  React 19 type fixes; `tsconfig.json` updated by the Next 16 toolchain. The
  vestigial `lint` script was removed (`next lint` no longer exists in 16 and
  ESLint was never configured in this repo — a linter can be added
  deliberately later if wanted). Route table unchanged in kind: site pages
  static, skill pages SSG, diff + raw routes dynamic. (#28)
