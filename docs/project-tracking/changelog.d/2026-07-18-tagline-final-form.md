### Changed

- **Tagline final form (Option A)** — both page eyebrows now render the
  generalized `SOLID · VSA · per-stack crucible reviews · Claude Code + Cursor`
  from the shared `site-tagline` constant; the layout metadata description
  matches. Enumeration moved off the header: the "One method, per-stack
  dialects" callout gains a tier sentence (frontend: Bulletproof React ·
  VSA/Blazor — backend/Python: FastAPI · Django · ML) linking to the live
  Skills page. Adding a future crucible variant needs zero header edits. (#164)
- **Registry chips name every dialect's stack** — `stackOf` now knows the
  `fastapi` / `django` / `ml` suffixes (deferred here from #161–#163), so no
  crucible variant's chip reads "any stack". Stale two-stack enumerations
  swept: Setup's "Pick your stack" note gains the Python tier, the Overview
  instruments line generalizes to "per-stack crucibles", and the README
  purpose line + skills tree cover all five dialects. (#164)
