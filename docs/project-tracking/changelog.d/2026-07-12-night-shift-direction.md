# Night-shift direction (docs + site + skills)

- **GITHUB-PROJECTS.md** — Models keys, shared-file / worktree rules, daytime vs
  night-shift Actions (personal per-repo vs org), implementation checklist → board
  issues.
- **Registry site** — `/night-shift` page + nav; overview / setup / flows updated;
  `CLAUDE_CODE_OAUTH_TOKEN` (not API key) in copy.
- **Skills** — thin night-shift (daytime chain); start-feature plan→execute +
  unattended consult; update-docs dating rulebook; finish-feature / history /
  parallel-worktree aligned.
- **Actions** — slim workflow prompts; `--permission-mode bypassPermissions`.
- **README** — Vilya = Dev Loop system; daytime primary.
