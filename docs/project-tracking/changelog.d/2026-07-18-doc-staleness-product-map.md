### Fixed

- **Two stale doc lines caught by the /product-map demo run** —
  `docs/specs/night-agent-viz.md` `Status:` now records the ship (PR #58 merged,
  #36 closed, page live at `/night-shift`) instead of "awaiting PR merge";
  `README.md`'s live-site nav line now mirrors the actual site nav (Overview ·
  Architect · Orchestrator · Skills · Setup · Night shift · Cursor vs Claude
  Code), picking up the Flows→Orchestrator rename and the two pages the old
  line was missing. (#152)
