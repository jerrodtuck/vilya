# Role prompt cards: invoke skill first

Graduated role-page prompt cards now surface **Apply prompt below — or run `/vilya-<skill>`** as the primary affordance; Copy stays as fallback. Shared `PromptItem.skill` + `PromptList` render the line; ungraduated seats (Cursor orchestrator, Architect standing orders) stay paste-only until #224.
