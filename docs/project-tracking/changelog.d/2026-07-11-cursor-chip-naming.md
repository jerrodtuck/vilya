### Changed

- **Cursor orchestrator kickoff prompt** now tells the orchestrator to title
  every agent chat (chip) it kicks off with its worktree folder name — exactly
  `<issue#>-<slug>` — so chips map 1:1 to worktrees at a glance. (#39)
