# Setup + Differences — Planner model routing

Setup Plan→execute and Differences plan/model rows now teach Planner-session
truth: standing Fable Planner writes `plan:ready`; chips inherit Sonnet from
`settings.local.json`; daytime may skip Planner; night-shift needs
`plan:ready` ∧ `night-shift:ready`. Retires the #89 orchestrator-`/model`-as-planner
story and residual `auto:ready` on those pages.
